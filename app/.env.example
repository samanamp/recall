# Optional. The app talks to /api on its own origin by default (the worker
# serves both). Set this only if the app is hosted separately from the worker
# (e.g. Cloudflare Pages, or `vite dev` without the /api proxy).
# VITE_WORKER_URL=https://recall.yourname.workers.dev
